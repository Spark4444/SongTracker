<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/detail.css">
</head>
<body>
    <%- include("partials/nav.ejs") %>
    <div class="detail-container">
        <h1><%= artist.name || "Unknown Artist" %></h1>
        
        <div class="detail-section">
            <h2>Artist Information</h2>
            <p><strong>Type:</strong> <%= artist.type || "N/A" %></p>
            <p><strong>Country:</strong> <%= artist.country || "N/A" %></p>
            <% if (artist["life-span"] && (artist["life-span"].begin || artist["life-span"].end)) { %>
                <p><strong>Active:</strong> 
                    <%= artist["life-span"].begin || "?" %> - <%= artist["life-span"].end || "Present" %>
                </p>
            <% } %>
            <% if (artist.disambiguation) { %>
                <p><strong>Description:</strong> <%= artist.disambiguation %></p>
            <% } %>
        </div>

        <% if (releaseGroups && releaseGroups.length > 0) { %>
        <div class="detail-section">
            <h2>Albums & EPs</h2>
            <ul class="releases-list">
            <% 
                releaseGroups.forEach(rg => { 
            %>
                <li>
                    <a href="/albums/<%= rg.id %>"><%= rg.title %></a>
                    <% if (rg["first-release-date"]) { %>
                        (<%= rg["first-release-date"].split("-")[0] %>)
                    <% } %>
                    <% if (rg.type) { %>
                        <span class="release-type">[<%= rg.type %>]</span>
                    <% } %>
                </li>
            <% }); %>
            </ul>
        </div>
        <% } %>

        <a href="/artists/search" class="back-link">← Back to Search</a>
    </div>
</body>
</html>
