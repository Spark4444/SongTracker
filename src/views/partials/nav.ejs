<nav>
    <ul>
        <% links.forEach(function(link) { %>
            <% if (link.text !== title) { %>
            <li><a href="<%= link.href %>"><%= link.text %></a></li>
            <% } %>
        <% }); %>
    </ul>
</nav>